#include <stdio.h>
#include <stdlib.h>
#include <omp.h>

#pragma imp parallel
{
    int z;
    z =
    z = z * z + c
}

int main(){

/*
#pragma omp parallel
    {
#pragma omp critical 
     {
      printf("hello from thread %d\n",omp_get_thread_num());
     }
    printf("inside parallel region %d\n", omp_get_num_threads());
    }
    printf("outside parallel region %d\n", omp_get_num_threads());
*/
}


